language: r
sudo: required
cache: packages

r_packages:
 - devtools
 - DT

r_github_packages:
  - UKGov-Data-Science/RAP-demo

before_script:
  - chmod +x ./_build.sh
  - chmod +x ./_deploy.sh

script:
  - ./_build.sh
  - ./_deploy.sh
